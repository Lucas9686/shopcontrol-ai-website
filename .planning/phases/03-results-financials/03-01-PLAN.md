---
phase: 03-results-financials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/de.json
  - messages/en.json
  - components/results/revenue-forecast-chart.tsx
  - components/results/pricing-comparison-chart.tsx
  - components/results/scenario-chart.tsx
  - app/[locale]/results/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to /results and see project results and market differentiation content"
    - "User can see three financial charts: pricing model, 3-year revenue forecast, and scenario analysis"
    - "All text content switches correctly between DE and EN"
  artifacts:
    - path: "app/[locale]/results/page.tsx"
      provides: "Results & Financials page with static content sections and chart islands"
      min_lines: 80
    - path: "components/results/revenue-forecast-chart.tsx"
      provides: "3-year revenue/cost area chart using shadcn ChartContainer"
      min_lines: 30
    - path: "components/results/pricing-comparison-chart.tsx"
      provides: "Pricing tiers bar chart"
      min_lines: 30
    - path: "components/results/scenario-chart.tsx"
      provides: "Optimistic/realistic/pessimistic scenario comparison chart"
      min_lines: 30
    - path: "messages/de.json"
      provides: "German translations for results page"
      contains: "results"
    - path: "messages/en.json"
      provides: "English translations for results page"
      contains: "results"
  key_links:
    - from: "app/[locale]/results/page.tsx"
      to: "components/results/*.tsx"
      via: "import and render with translated label props"
      pattern: "import.*components/results"
    - from: "app/[locale]/results/page.tsx"
      to: "messages/*.json"
      via: "getTranslations('results')"
      pattern: "getTranslations.*results"
---

<objective>
Create the Results & Financials page with project results, market differentiation content, and three interactive financial charts (pricing model, 3-year revenue forecast, scenario analysis) in both DE and EN.

Purpose: Demonstrates business viability to the Jugend Innovativ jury (Entrepreneurship category) with visual financial projections.
Output: Navigable /results page with static content sections and three Recharts-powered chart components using shadcn ChartContainer.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Lucas Benutzer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-results-financials/03-RESEARCH.md

# Existing page patterns to follow:
@app/[locale]/technology/page.tsx
@components/shared/section-header.tsx
@components/shared/fade-in-section.tsx
@components/ui/chart.tsx

# Content source for financial data:
@finanzplan_diagramm_schoen.png
@da_webshop_agent_bericht.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add results page translation keys to DE and EN</name>
  <files>messages/de.json, messages/en.json</files>
  <action>
Add a "results" top-level key to both messages/de.json and messages/en.json. Structure:

```json
"results": {
  "title": "Ergebnisse & Finanzplan" / "Results & Financials",
  "subtitle": "..." (brief intro sentence),
  "market": {
    "title": "Ergebnisse & Marktabgrenzung" / "Results & Market Differentiation",
    "subtitle": "...",
    "results": {
      "title": "Projektergebnisse" / "Project Results",
      "description": "..." (what ShopControl AI achieved: functional support agent, automated bookkeeping, Shopify integration, tested with real shop)
    },
    "differentiation": {
      "title": "Marktabgrenzung" / "Market Differentiation",
      "description": "..." (how ShopControl AI differs: combines support + bookkeeping in one, built for small shops, affordable vs enterprise solutions like Zendesk/Freshdesk)
    },
    "competitors": {
      "title": "Wettbewerbsvergleich" / "Competitive Comparison",
      "shopcontrol": "ShopControl AI",
      "features": ["KI-Support-Agent", "Buchhaltungsautomatisierung", "Shopify-Integration", "Preislich fuer kleine Shops"] / EN equivalents
    }
  },
  "financials": {
    "title": "Finanzplan" / "Financial Plan",
    "subtitle": "...",
    "pricing": {
      "title": "Preismodell" / "Pricing Model",
      "description": "..."
    },
    "forecast": {
      "title": "3-Jahres-Prognose" / "3-Year Forecast",
      "description": "..."
    },
    "scenario": {
      "title": "Szenarioanalyse" / "Scenario Analysis",
      "description": "..."
    }
  },
  "charts": {
    "revenue": "Umsatz" / "Revenue",
    "costs": "Kosten" / "Costs",
    "profit": "Gewinn" / "Profit",
    "year1": "Jahr 1" / "Year 1",
    "year2": "Jahr 2" / "Year 2",
    "year3": "Jahr 3" / "Year 3",
    "optimistic": "Optimistisch" / "Optimistic",
    "realistic": "Realistisch" / "Realistic",
    "pessimistic": "Pessimistisch" / "Pessimistic",
    "basic": "Basic",
    "pro": "Pro",
    "enterprise": "Enterprise",
    "pricePerMonth": "Preis/Monat" / "Price/Month",
    "currency": "€"
  }
}
```

Content guidance: Reference the project report (da_webshop_agent_bericht.pdf) and finanzplan_diagramm_schoen.png for accurate financial figures. If exact numbers aren't extractable, use reasonable placeholder data consistent with a SaaS startup targeting small Shopify shops (Basic ~29€/mo, Pro ~79€/mo, Enterprise ~199€/mo; Year 1 revenue ~12k€, Year 2 ~36k€, Year 3 ~72k€).

IMPORTANT: Do NOT put numeric chart data in translation files. Only labels and descriptions go here.
  </action>
  <verify>Both de.json and en.json parse as valid JSON. The "results" key exists in both with matching structure. Run: `node -e "const d=require('./messages/de.json'); const e=require('./messages/en.json'); console.log(!!d.results && !!e.results && !!d.results.charts && !!e.results.charts);"` — should print `true`.</verify>
  <done>Both translation files contain complete results page keys with all section titles, descriptions, and chart labels in DE and EN.</done>
</task>

<task type="auto">
  <name>Task 2: Create three client chart components</name>
  <files>components/results/revenue-forecast-chart.tsx, components/results/pricing-comparison-chart.tsx, components/results/scenario-chart.tsx</files>
  <action>
Create three "use client" chart components in components/results/. Each uses shadcn ChartContainer + ChartConfig + ChartTooltip from @/components/ui/chart. Each receives translated labels as props from the server page.

**revenue-forecast-chart.tsx** — 3-year revenue vs costs area/line chart:
- Data: 3 data points (Year 1, 2, 3) with revenue and costs columns
- Use AreaChart with two Area elements (revenue in --chart-1, costs in --chart-2)
- CartesianGrid, XAxis (year labels from props), YAxis with €-formatter
- ChartTooltip + ChartLegend
- Wrap in Card with CardHeader (title/description from props)
- Financial data: Year 1: revenue 14400€ costs 5400€, Year 2: revenue 43200€ costs 12000€, Year 3: revenue 86400€ costs 20000€ (reasonable SaaS growth)

**pricing-comparison-chart.tsx** — pricing tiers bar chart:
- Data: 3 tiers (Basic 29€, Pro 79€, Enterprise 199€) with feature count
- Use BarChart with Bar element (--chart-1 color)
- XAxis (plan names from props), YAxis with €-formatter
- ChartTooltip
- Wrap in Card with CardHeader

**scenario-chart.tsx** — scenario analysis grouped bar chart:
- Data: 3 years x 3 scenarios (optimistic, realistic, pessimistic revenue)
- Use BarChart with 3 grouped Bar elements (--chart-1 optimistic, --chart-2 realistic, --chart-3 pessimistic)
- XAxis (year labels), YAxis with €-formatter
- ChartTooltip + ChartLegend
- Wrap in Card with CardHeader
- Optimistic: 20k/60k/120k, Realistic: 14k/43k/86k, Pessimistic: 8k/20k/40k

All components:
- MUST have "use client" directive
- Use `className="min-h-[300px] w-full"` on ChartContainer (avoid aspect ratio issues on mobile)
- Use CSS variable colors from ChartConfig (never hard-coded)
- Export named function (not default)
- Props interface with title, description, and labels object
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors in the three new files. Each file starts with "use client" directive.</verify>
  <done>Three chart components exist, each renders a themed Recharts chart inside shadcn ChartContainer with translated labels via props.</done>
</task>

<task type="auto">
  <name>Task 3: Create results page with content sections and chart islands</name>
  <files>app/[locale]/results/page.tsx</files>
  <action>
Create the results page as an async server component following the exact pattern of existing pages (technology/page.tsx).

Structure:
1. Standard async page signature with `params: Promise<{ locale: string }>`, setRequestLocale, getTranslations("results")
2. `<article className="mx-auto max-w-5xl px-4 pt-24 pb-16">`
3. Header section: h1 title + subtitle (centered, same styling as other pages)
4. Results & Market Differentiation section:
   - SectionHeader for section title
   - FadeInSection wrapping content
   - Two Card components side-by-side (md:grid-cols-2): Project Results + Market Differentiation
   - Each card: icon (TrendingUp, Target from lucide-react), title, description paragraph
   - Optional: brief competitive comparison list (ShopControl AI vs generic tools)
5. Financial Projections section:
   - SectionHeader for section title
   - Three chart components in a grid or stacked layout:
     - PricingComparisonChart (pricing tiers)
     - RevenueForecastChart (3-year forecast)
     - ScenarioChart (scenario analysis)
   - Each wrapped in FadeInSection
   - Pass translated labels from t("charts.*") as props to each chart

Import charts from @/components/results/. Import SectionHeader and FadeInSection from @/components/shared/. Import Card components from @/components/ui/card.

Do NOT add "use client" to this file. It is a server component.

Add navbar link: Check if the navbar already has a results link (it should per research). If not, add it.
  </action>
  <verify>Run `npm run build` or `npx next build` — the /results route builds without errors. Navigate to /de/results and /en/results — page renders with content and charts.</verify>
  <done>Results page renders at /[locale]/results with market differentiation content and three financial charts. Content switches between DE and EN.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` completes successfully with /results route
3. Navigate to /de/results — German content visible, three charts render
4. Navigate to /en/results — English content visible, charts render
5. Charts display themed colors (dark mode compatible via CSS variables)
6. Page follows same layout pattern as other content pages (max-w-5xl, SectionHeader, FadeInSection)
</verification>

<success_criteria>
- Results page accessible at /de/results and /en/results
- Page shows project results and market differentiation text content
- Three financial charts render: pricing model, revenue forecast, scenario analysis
- All text content available in both German and English
- Charts use shadcn ChartContainer with theme-aware colors
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-results-financials/03-01-SUMMARY.md`
</output>

---
phase: 09-content-updates-verification
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - webapp/app/[locale]/page.tsx
  - webapp/messages/de.json
  - webapp/messages/en.json
autonomous: true

must_haves:
  truths:
    - "Homepage shows overview section with links to Support-Agent, Buchhaltungstool, Ergebnisse, and Finanzplan"
    - "Homepage CTAs navigate to correct pages"
  artifacts:
    - path: "webapp/app/[locale]/page.tsx"
      provides: "Updated homepage with overview CTA grid"
      min_lines: 60
  key_links:
    - from: "webapp/app/[locale]/page.tsx"
      to: "/support-agent"
      via: "Link component href"
      pattern: "href.*support-agent"
    - from: "webapp/app/[locale]/page.tsx"
      to: "/finance-plan"
      via: "Link component href"
      pattern: "href.*finance-plan"
---

<objective>
Add an overview/CTA section to the homepage that links to the key pages (Support-Agent, Buchhaltungstool, Ergebnisse, Finanzplan), giving visitors a clear navigation path to all content.

Purpose: CONTENT-01 requirement — Startseite CTAs and links reflect new site structure.
Output: Updated homepage with overview grid section.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Lucas Benutzer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-content-updates-verification/09-RESEARCH.md
@webapp/app/[locale]/page.tsx
@webapp/messages/de.json (hero section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add homepage overview translations and build CTA grid</name>
  <files>webapp/messages/de.json, webapp/messages/en.json, webapp/app/[locale]/page.tsx</files>
  <action>
**Step 1: Add translation keys** in both DE and EN under a new `overview` key (top-level, alongside `hero` and `demo`):

```json
"overview": {
  "title": "Was ShopControl AI bietet",
  "subtitle": "Zwei KI-gestützte Systeme für Online-Shops",
  "supportAgent": {
    "title": "KI-Support-Agent",
    "description": "Vollautonome E-Mail-Beantwortung mit RAG-System und Shopify-Integration."
  },
  "accounting": {
    "title": "Buchhaltungstool",
    "description": "Automatische Finanzberichte per WhatsApp — Umsatz, Gewinn, Trends."
  },
  "results": {
    "title": "Ergebnisse",
    "description": "Erreichte Ergebnisse, Marktabgrenzung und Geschäftsmodell."
  },
  "financePlan": {
    "title": "Finanzplan",
    "description": "Kostenstruktur, Preismodell und 3-Jahres-Wachstumsprognose."
  }
}
```

**Step 2: Update homepage** (`page.tsx`):

Add a new section between the Hero and the Demo Video sections. Use the pattern from 09-RESEARCH.md:

```tsx
const to = await getTranslations("overview");
```

Then add a `<section>` with:
- `SectionHeader` with overview title/subtitle
- 2x2 grid of `Link` cards (from `@/i18n/navigation`), each linking to `/support-agent`, `/accounting`, `/results`, `/finance-plan`
- Each card shows title + description from translations
- Card styling: `rounded-lg border border-border/50 bg-card/50 p-6 transition-colors hover:border-primary/30 hover:bg-card`
- Use `ArrowRight` icon from lucide-react on each card

Also update the Hero CTA: change `href="/technology"` to `href="/support-agent"` since that's now the primary entry point.
  </action>
  <verify>`cd webapp && npx next build` succeeds. Visit /de — overview section visible with 4 cards linking to correct pages.</verify>
  <done>Homepage shows overview grid with 4 CTA cards. Hero CTA links to /support-agent. All links work in DE and EN.</done>
</task>

</tasks>

<verification>
1. Build succeeds
2. Homepage shows overview section with 4 linked cards
3. Each card links to correct page (/support-agent, /accounting, /results, /finance-plan)
4. Hero CTA updated to /support-agent
5. DE and EN both render correctly
</verification>

<success_criteria>
- Homepage has overview section with links to all 4 key pages
- Hero CTA points to /support-agent
- Card grid is responsive (2 cols on md+, 1 on mobile)
- Both languages complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-content-updates-verification/09-03-SUMMARY.md`
</output>

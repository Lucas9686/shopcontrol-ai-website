---
phase: 09-content-updates-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/app/[locale]/results/page.tsx
  - webapp/messages/de.json
  - webapp/messages/en.json
autonomous: true

must_haves:
  truths:
    - "Results page shows Erreichte Ergebnisse section"
    - "Results page shows Marktabgrenzung section"
    - "Results page shows Zielgruppe section"
    - "Results page shows Geschäftsmodell overview (without Finanzplan details)"
    - "Results page does NOT contain financial charts"
  artifacts:
    - path: "webapp/app/[locale]/results/page.tsx"
      provides: "Restructured Results page without financial charts"
      min_lines: 60
  key_links:
    - from: "webapp/app/[locale]/results/page.tsx"
      to: "webapp/messages/de.json"
      via: "getTranslations('results')"
      pattern: "getTranslations.*results"
---

<objective>
Restructure the Results page to remove financial charts (moved to Finanzplan in plan 01) and add Zielgruppe and Geschaeftsmodell sections. The page should focus on achieved results, market positioning, and business model overview.

Purpose: PAGE-04 requirement — Results page shows Ergebnisse, Marktabgrenzung, Zielgruppe, Geschaeftsmodell without Finanzplan.
Output: Updated results page and translation keys.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Lucas Benutzer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-content-updates-verification/09-RESEARCH.md
@webapp/app/[locale]/results/page.tsx
@webapp/messages/de.json (results section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update results translation keys and add new sections</name>
  <files>webapp/messages/de.json, webapp/messages/en.json</files>
  <action>
In both DE and EN translation files, update the `results` object:

1. KEEP: `title`, `subtitle`, `market.title`, `market.subtitle`, `market.results.*`, `market.differentiation.*`
2. REMOVE: `financials.*` section entirely, `charts.*` section entirely (these will live under `financePlan` from plan 01)
3. ADD new sections:

```
results: {
  // ... keep existing market section ...

  targetGroup: {
    title: "Zielgruppe",
    subtitle: "Wer profitiert von ShopControl AI",
    primary: {
      title: "Kleine und mittlere Online-Shops",
      description: "Shopify-Händler mit 50–500 Bestellungen/Monat, die Support-Kosten senken und Finanzen automatisieren wollen."
    },
    secondary: {
      title: "Wachsende E-Commerce-Unternehmen",
      description: "Shops, die skalieren wollen, ohne proportional mehr Support-Personal einzustellen."
    }
  },

  businessModel: {
    title: "Geschäftsmodell",
    subtitle: "SaaS mit hoher Marge und geringen variablen Kosten",
    description: "ShopControl AI wird als monatliches Abo vertrieben. Die KI-gestützten Agenten laufen auf bestehender Cloud-Infrastruktur — die variablen Kosten pro Kunde liegen bei 50–80 EUR/Monat, was Margen von 60–90% ermöglicht.",
    cta: "Zum Finanzplan",
    ctaHref: "/finance-plan"
  },

  achievements: {
    title: "Erreichte Ergebnisse",
    subtitle: "Was im Rahmen der Diplomarbeit umgesetzt wurde",
    items: [
      "Vollautonomer E-Mail-Support-Agent mit 6-Schritte-Workflow",
      "RAG-System mit Pinecone-Vektordatenbank für kontextbasierte Antworten",
      "Automatisierte Buchhaltung mit WhatsApp-KPI-Reporting",
      "3 spezialisierte Sub-Agents (Shopify, Sheets, Rechnungs-Agent)",
      "4-stufiges Sicherheitssystem gegen Halluzinationen",
      "Gewerbeschein erhalten, SaaS-Launch geplant"
    ]
  }
}
```

Create matching EN translations. Content must match Projektbericht.
  </action>
  <verify>JSON parse check for both files succeeds.</verify>
  <done>Results translation keys updated: financials/charts removed, targetGroup/businessModel/achievements added.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Results page without charts</name>
  <files>webapp/app/[locale]/results/page.tsx</files>
  <action>
Rewrite the Results page to remove all chart imports and financial sections. New structure:

1. Header (title + subtitle)
2. Erreichte Ergebnisse section — bulleted list or card grid showing achievement items from `results.achievements.items` (use `t.raw("achievements.items")` to get the array, then map over it). Use CheckCircle icon from lucide-react.
3. Market section — keep existing 2-card layout (results + differentiation), unchanged.
4. Zielgruppe section — 2 cards (primary + secondary target groups).
5. Geschaeftsmodell section — description text + CTA link to /finance-plan using `Link` from `@/i18n/navigation`.

REMOVE: All chart component imports (PricingComparisonChart, RevenueForecastChart, ScenarioChart). Remove the entire "Financial Plan" FadeInSection blocks.

Use existing patterns: FadeInSection, SectionHeader, Card/CardContent/CardHeader/CardTitle.
  </action>
  <verify>`cd webapp && npx next build` — no errors. Visit /de/results — no charts visible, new sections render.</verify>
  <done>Results page shows Ergebnisse, Markt, Zielgruppe, Geschaeftsmodell sections. No financial charts. CTA links to /finance-plan.</done>
</task>

</tasks>

<verification>
1. Build succeeds
2. Results page has NO chart imports or chart rendering
3. All 4 sections render: Achievements, Market, Zielgruppe, Geschaeftsmodell
4. CTA to /finance-plan works
5. Both DE and EN load correctly
</verification>

<success_criteria>
- Results page restructured without any financial charts
- New sections: Erreichte Ergebnisse, Zielgruppe, Geschaeftsmodell
- Existing Market section preserved
- Link to Finanzplan page present
- DE + EN complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-content-updates-verification/09-02-SUMMARY.md`
</output>

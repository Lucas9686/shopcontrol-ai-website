---
phase: 02-core-content-pages
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/[locale]/technology/page.tsx
  - components/technology/workflow-step.tsx
  - components/technology/screenshot-figure.tsx
  - messages/de.json
  - messages/en.json
autonomous: true

must_haves:
  truths:
    - "User can explore the technical deep-dive with 6 workflow steps, RAG explanation, tools, and security -- with diagrams and screenshots"
  artifacts:
    - path: "app/[locale]/technology/page.tsx"
      provides: "Technology deep-dive page"
      min_lines: 80
    - path: "components/technology/workflow-step.tsx"
      provides: "Numbered workflow step with screenshot"
      min_lines: 20
    - path: "components/technology/screenshot-figure.tsx"
      provides: "Image figure with caption"
      min_lines: 15
  key_links:
    - from: "app/[locale]/technology/page.tsx"
      to: "messages/de.json"
      via: "getTranslations('technology')"
      pattern: "getTranslations.*technology"
    - from: "app/[locale]/technology/page.tsx"
      to: "public/images/workflows/"
      via: "next/image src"
      pattern: "/images/workflows/"
---

<objective>
Create the technology deep-dive page with 6 workflow steps, RAG system explanation, tools/sub-agents section, and security features -- all with screenshots and diagrams.

Purpose: Delivers CONT-03 -- the most technically detailed page, showing the jury exactly how ShopControl AI works under the hood.
Output: Technology page with workflow timeline, screenshots, and technical explanations in DE and EN.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Lucas Benutzer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-content-pages/02-RESEARCH.md
@.planning/phases/02-core-content-pages/02-01-SUMMARY.md
@app/[locale]/page.tsx
@messages/de.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create technology components</name>
  <files>
    components/technology/workflow-step.tsx
    components/technology/screenshot-figure.tsx
  </files>
  <action>
1. Create components/technology/workflow-step.tsx:
   - Props: number (number), title (string), description (string), imageSrc (string), imageAlt (string), reverse (boolean, optional — flips image to left side on even steps)
   - Grid layout: md:grid-cols-2 with text on one side, screenshot on other
   - Number badge: rounded-full bg-primary text-primary-foreground w-8 h-8 flex items-center justify-center font-bold
   - Wrap in FadeInSection (from components/shared/fade-in-section.tsx)
   - Server component (no "use client") — FadeInSection handles client animation

2. Create components/technology/screenshot-figure.tsx:
   - Props: src (string), alt (string), caption (string, optional), width (number, default 1200), height (number, default 675)
   - Uses next/image inside figure element
   - Rounded border (rounded-lg border border-border overflow-hidden)
   - Optional figcaption (bg-muted/50 px-4 py-2 text-sm text-muted-foreground)
  </action>
  <verify>Files exist and TypeScript compiles: npx tsc --noEmit</verify>
  <done>WorkflowStep and ScreenshotFigure components are created and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Create technology page with translations</name>
  <files>
    app/[locale]/technology/page.tsx
    messages/de.json
    messages/en.json
  </files>
  <action>
1. Add "technology" namespace to messages/de.json with these sections:

   a) Header: title "Technologie", subtitle "So funktioniert ShopControl AI unter der Haube"

   b) Overview: Brief intro explaining n8n-based architecture with two main systems

   c) 6 Workflow Steps (the core of the page — from the Projektbericht):
      - Step 1: "Kundenanfrage empfangen" — Chat-Widget/E-Mail kommt rein, n8n Trigger startet Workflow
        Image: /images/workflows/gesamter-workflow.png
      - Step 2: "KI-Agent analysiert" — GPT-4 Agent versteht die Anfrage, entscheidet welches Tool nötig ist
        Image: /images/workflows/support-agent-mit-tools.png
      - Step 3: "RAG-Wissensabfrage" — Pinecone Vektordatenbank liefert relevante Shop-Dokumente
        Image: /images/workflows/rag-auto-upload.png
      - Step 4: "Shopify-Datenabfrage" — Sub-Agent greift auf Bestellungen, Produkte, Kundendaten zu
        Image: /images/workflows/tools-von-agent.png
      - Step 5: "Antwort generieren" — Agent formuliert personalisierte Antwort basierend auf allen Daten
        Image: /images/workflows/support-agent-ohne-tools.png
      - Step 6: "Buchhaltung automatisch" — Rechnungen erstellt, PDFs generiert, per E-Mail versendet, in Sheets geloggt
        Image: /images/diagrams/buchhaltung-workflow.png

   d) RAG section: title, explanation of Retrieval-Augmented Generation, how documents are embedded into Pinecone, auto-upload workflow
      Image: /images/workflows/rag-upload.png

   e) Tools & Sub-Agents section: title, list of tools the agent can use:
      - Shopify-Agent: Bestellungen, Produkte, Kunden abfragen
      - Google Sheets-Agent: Buchhaltungsdaten lesen/schreiben
      - E-Mail-Tool: Rechnungen und Benachrichtigungen versenden
      - Bildanalyse: Produktbilder analysieren
      Image: /images/workflows/bildanalyse-sektion.png

   f) Security section: title, description of guardrails:
      - Prompt Injection Schutz
      - Fehler-Trigger mit Benachrichtigung
      - Eskalation an menschlichen Support
      Image: /images/workflows/error-trigger.png

2. Add equivalent English translations for "technology" namespace.

3. Create app/[locale]/technology/page.tsx:
   - Async server component, getTranslations("technology")
   - Layout: article with mx-auto max-w-5xl px-4 pt-24 pb-16 (wider than problem/solution for screenshots)
   - Centered header (SectionHeader)
   - Overview paragraph
   - Workflow section: h2 + 6 WorkflowStep components in sequence, alternating layout (reverse prop on even numbers) with vertical spacing (space-y-16)
   - RAG section: SectionHeader + explanation text + ScreenshotFigure
   - Tools section: SectionHeader + grid of 4 tool cards (reuse FeatureCard with icons: ShoppingCart, FileSpreadsheet, Mail, Image from lucide-react) + ScreenshotFigure
   - Security section: SectionHeader + 3 items in a grid + ScreenshotFigure
   - All major sections wrapped in FadeInSection
  </action>
  <verify>
    - npm run build succeeds
    - /de/technology renders with 6 workflow steps and screenshots
    - /en/technology renders with English translations
    - All images load (no broken images)
  </verify>
  <done>Technology page shows 6 workflow steps with screenshots, RAG explanation, tools/sub-agents, and security features. Fully bilingual.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- /de/technology shows all 6 workflow steps with German text and screenshots
- /en/technology shows English version
- Navbar link to /technology works
- Screenshots load from /images/workflows/ and /images/diagrams/
- RAG, tools, and security sections all visible
</verification>

<success_criteria>
Technology deep-dive page renders 6 workflow steps with alternating layout and screenshots, plus RAG, tools, and security sections. All content bilingual. All screenshots load.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-content-pages/02-02-SUMMARY.md`
</output>

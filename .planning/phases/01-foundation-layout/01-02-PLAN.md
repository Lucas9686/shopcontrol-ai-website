---
phase: 01-foundation-layout
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/shared/navbar.tsx
  - components/shared/footer.tsx
  - components/shared/language-switcher.tsx
  - app/[locale]/layout.tsx
  - app/[locale]/page.tsx
autonomous: false

must_haves:
  truths:
    - "User sees a responsive navbar with navigation links on desktop and hamburger menu on mobile"
    - "User can click DE/EN toggle and URL switches locale while staying on same page"
    - "User sees hero section with project headline, subheadline, and CTA button"
    - "User sees footer with project info and HTL reference"
    - "Site is live on Vercel with working i18n and optimized images"
  artifacts:
    - path: "components/shared/navbar.tsx"
      provides: "Responsive navigation bar with mobile menu"
      min_lines: 40
    - path: "components/shared/language-switcher.tsx"
      provides: "DE/EN locale toggle"
      contains: "useRouter"
    - path: "components/shared/footer.tsx"
      provides: "Site footer"
      min_lines: 20
    - path: "app/[locale]/page.tsx"
      provides: "Landing page with hero section"
      min_lines: 30
  key_links:
    - from: "components/shared/language-switcher.tsx"
      to: "i18n/navigation.ts"
      via: "useRouter + usePathname"
      pattern: "router\\.replace.*locale"
    - from: "components/shared/navbar.tsx"
      to: "components/shared/language-switcher.tsx"
      via: "import and render"
      pattern: "LanguageSwitcher"
    - from: "app/[locale]/layout.tsx"
      to: "components/shared/navbar.tsx"
      via: "import Navbar"
      pattern: "<Navbar"
---

<objective>
Build responsive navbar with language switcher, hero section, and footer -- completing the bilingual site shell.

Purpose: Delivers all visible UI for Phase 1 -- users can navigate, switch languages, and see the landing page.
Output: Complete site shell with navbar, hero, footer, and working language switching.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Lucas Benutzer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-layout/01-RESEARCH.md
@.planning/phases/01-foundation-layout/01-01-SUMMARY.md
@Web design/components/navbar.tsx
@Web design/components/hero.tsx
@Web design/components/footer.tsx
@messages/de.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navbar with language switcher and footer</name>
  <files>
    components/shared/navbar.tsx
    components/shared/language-switcher.tsx
    components/shared/footer.tsx
    app/[locale]/layout.tsx
  </files>
  <action>
1. Create `components/shared/language-switcher.tsx` ("use client"):
   - Import useLocale from next-intl
   - Import usePathname, useRouter from @/i18n/navigation
   - Render a button showing the OTHER locale (if on DE, show "EN" and vice versa)
   - On click: `router.replace(pathname, { locale: otherLocale })`
   - Style: small pill/badge button with border, subtle hover effect

2. Create `components/shared/navbar.tsx` ("use client"):
   - Reference the existing template navbar at `Web design/components/navbar.tsx` for design patterns
   - Use useTranslations('nav') for link labels
   - Desktop: horizontal nav links (Home, Problem, Solution, Technology, Results, About) + LanguageSwitcher on the right
   - Mobile: hamburger icon that opens a shadcn/ui Sheet (slide-out menu) with all nav links + LanguageSwitcher
   - Use the Link from @/i18n/navigation for locale-aware routing
   - Links point to: /, /problem, /solution, /technology, /results, /about (these pages don't exist yet -- that's fine, they'll be Phase 2)
   - Sticky top with backdrop blur, dark blue background matching theme
   - Show "ShopControl AI" as logo text on the left

3. Create `components/shared/footer.tsx`:
   - Use useTranslations('footer')
   - Show copyright line and HTL Klagenfurt reference
   - Simple centered layout, muted text color
   - Include a placeholder for HTL logo (Next.js Image component with alt text, actual image can be added later)

4. Update `app/[locale]/layout.tsx`:
   - Import and render Navbar above main, Footer below main
   - Keep all existing i18n setup from Plan 01
  </action>
  <verify>
Run `pnpm dev`. Verify: navbar visible with links, language switcher toggles between /de and /en, footer visible at bottom. Resize browser to mobile width -- hamburger menu appears. Run `pnpm build`.
  </verify>
  <done>
Responsive navbar renders with nav links and language switcher. Footer visible. Mobile hamburger menu works. Language toggle switches locale in URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build hero section on landing page</name>
  <files>
    app/[locale]/page.tsx
    messages/de.json
    messages/en.json
  </files>
  <action>
1. Update `app/[locale]/page.tsx` to render a full hero section:
   - Reference `Web design/components/hero.tsx` for design inspiration
   - Use useTranslations('hero') for headline, subheadline, CTA text
   - Layout: centered text, large headline (text-4xl md:text-6xl font-bold), subheadline below (text-lg md:text-xl text-muted-foreground), CTA button below (shadcn Button component, size lg)
   - CTA button links to "#features" or "/technology" (placeholder for now)
   - Add a subtle gradient or glow effect behind the hero text using a CSS radial gradient with the primary blue color
   - Use Framer Motion for a simple fade-in-up animation on the hero content (motion.div with initial opacity 0, y 20, animate to opacity 1, y 0)
   - Full viewport height hero (min-h-[80vh] flex items-center justify-center)
   - Import setRequestLocale and call it for static rendering support

2. Update messages/de.json and messages/en.json if any new translation keys are needed beyond what Plan 01 created.
  </action>
  <verify>
Run `pnpm dev`. Visit /de -- hero section fills viewport with German headline, subheadline, and CTA. Visit /en -- same in English. Animation plays on load. Run `pnpm build`.
  </verify>
  <done>
Hero section renders with translated headline, subheadline, and CTA button. Fade-in animation plays. Both locales display correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete bilingual site shell: dark blue theme, responsive navbar with language switcher, animated hero section, footer. Working i18n routing between /de and /en.</what-built>
  <how-to-verify>
1. Run `pnpm dev` and visit http://localhost:3000 -- should redirect to /de
2. Verify dark blue background (not gray/zinc)
3. Navbar: click navigation links, verify they appear correctly
4. Language switcher: click to toggle to EN, verify URL changes to /en and text updates
5. Hero: verify headline, subheadline, CTA button visible with fade-in animation
6. Footer: scroll down, verify project info and HTL reference
7. Mobile: resize to ~375px width, verify hamburger menu appears and works
8. Visit /en directly -- English content throughout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Deploy to Vercel</name>
  <files></files>
  <action>
1. Ensure the project has a git remote on GitHub (if not, create one with `gh repo create`).
2. Run `vercel --yes` to deploy to Vercel. If vercel CLI is not installed, run `pnpm add -g vercel` first.
3. After deployment, capture the production URL.
4. Visit the production URL and verify:
   - / redirects to /de
   - Dark blue theme renders correctly
   - Next.js Image optimization is working (check network tab for /_next/image responses)
   - Language switcher works on production
5. If deployment fails, check build logs and fix any issues.
  </action>
  <verify>
Production URL is accessible. Visit it in browser — site loads with dark blue theme, i18n routing works, images optimized.
  </verify>
  <done>
Site is live on Vercel with working i18n routing, dark blue theme, and optimized images.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds
2. / redirects to /de
3. Navbar responsive (desktop links + mobile hamburger)
4. Language switcher toggles /de <-> /en preserving path
5. Hero section with translated content and animation
6. Footer visible with project info
7. Dark blue theme consistent throughout
</verification>

<success_criteria>
- All 5 Phase 1 success criteria from ROADMAP.md are met:
  1. Responsive navbar works on mobile, tablet, desktop
  2. Language switch updates URL with locale prefix
  3. Hero section with headline and CTA on landing page
  4. Dark blue theme consistent with footer visible
  5. Site is live on Vercel (Task 4 deploys after visual approval)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-02-SUMMARY.md`
</output>

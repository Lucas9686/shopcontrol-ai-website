---
phase: 06-og-image-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/public/og-image.png
  - webapp/public/placeholder-logo.png
  - webapp/public/placeholder-logo.svg
  - scripts/generate-og-image.ts
autonomous: true

must_haves:
  truths:
    - "Sharing any page URL shows a branded ShopControl AI preview image at 1200x630"
    - "Website logo shows the real ShopControl AI logo, not a placeholder"
  artifacts:
    - path: "webapp/public/og-image.png"
      provides: "Branded OG image 1200x630"
    - path: "webapp/public/placeholder-logo.png"
      provides: "Real ShopControl AI logo"
  key_links:
    - from: "webapp/app/[locale]/layout.tsx"
      to: "webapp/public/og-image.png"
      via: "openGraph.images metadata"
      pattern: "og-image.png"
---

<objective>
Replace placeholder OG image and logo with branded ShopControl AI versions.

Purpose: Social sharing shows a professional branded preview instead of a placeholder. Website displays the real logo.
Output: Branded 1200x630 OG image composited from source logo onto dark background. Real logo replacing placeholder files.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Lucas Benutzer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-og-image-performance/06-RESEARCH.md
@webapp/app/[locale]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate branded OG image and replace logo</name>
  <files>
    webapp/public/og-image.png
    webapp/public/placeholder-logo.png
    webapp/public/placeholder-logo.svg
  </files>
  <action>
1. Create a Node.js script using `sharp` (already in devDependencies) to generate the OG image:
   - Canvas: 1200x630, background color rgb(9, 9, 11) — matches the site's dark theme (`--background` in globals.css)
   - Load `Matrealien/SchopControl OG logo.png` (256x290 source)
   - Resize logo to fit within ~400px height, maintaining aspect ratio
   - Composite centered on canvas
   - Save as `webapp/public/og-image.png` (PNG, optimized)

2. Run the script: `node --import tsx scripts/generate-og-image.ts` (tsx is in devDependencies)

3. Copy `Matrealien/ShcopControl Logo.png` to `webapp/public/placeholder-logo.png` (overwrite existing). Use sharp to optimize: resize to reasonable web size (~200px height), convert to PNG.

4. Remove `webapp/public/placeholder-logo.svg` since we now have a real PNG logo. If any component imports the SVG, update the import to use the PNG.

5. Verify the OG meta tags in `webapp/app/[locale]/layout.tsx` still point to `/og-image.png` with width 1200 and height 630 — these should already be correct per research.
  </action>
  <verify>
- `webapp/public/og-image.png` exists and is 1200x630 (check with: `node -e "const s=require('sharp'); s('webapp/public/og-image.png').metadata().then(m => console.log(m.width, m.height))"`)
- `webapp/public/placeholder-logo.png` exists and contains the real ShopControl AI logo
- `webapp/public/placeholder-logo.svg` is deleted (or updated)
- `npm run build` in webapp/ still succeeds
  </verify>
  <done>OG image is 1200x630 branded composite. Logo files replaced with real ShopControl AI branding. Build passes.</done>
</task>

</tasks>

<verification>
- OG image dimensions are exactly 1200x630
- Logo PNG exists and is optimized for web
- No broken imports referencing deleted SVG
- `npm run build` passes
</verification>

<success_criteria>
- webapp/public/og-image.png is a branded 1200x630 image with ShopControl AI logo on dark background
- webapp/public/placeholder-logo.png contains the real logo
- Build succeeds with no missing file errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-og-image-performance/06-01-SUMMARY.md`
</output>

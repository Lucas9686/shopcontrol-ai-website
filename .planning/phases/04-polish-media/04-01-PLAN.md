---
phase: 04-polish-media
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/shared/hero-animated.tsx
  - app/[locale]/page.tsx
  - app/globals.css
  - app/[locale]/layout.tsx
  - components/smooth-scroll.tsx
autonomous: true

must_haves:
  truths:
    - "Home page hero text animates in with staggered fade-up on load"
    - "Home page CTA button has a shimmer/glow effect"
    - "Smooth scroll (Lenis) is active across all pages"
    - "Content pages retain existing FadeInSection animations"
  artifacts:
    - path: "components/shared/hero-animated.tsx"
      provides: "Client-side animated hero wrapper with Framer Motion"
      contains: "motion"
    - path: "app/globals.css"
      provides: "Shimmer keyframes CSS animation"
      contains: "shimmer"
  key_links:
    - from: "app/[locale]/page.tsx"
      to: "components/shared/hero-animated.tsx"
      via: "import and render with translated strings as props"
      pattern: "HeroAnimated"
    - from: "app/[locale]/layout.tsx"
      to: "components/smooth-scroll.tsx"
      via: "SmoothScroll wrapping page content"
      pattern: "SmoothScroll"
---

<objective>
Add Framer Motion animations to the home page and integrate Lenis smooth scroll across all pages.

Purpose: The home page is currently static with no animations. Content pages already use FadeInSection, but the hero needs staggered fade-up text reveal and a shimmer effect on the CTA. Lenis smooth scroll exists as a component but is not wired into the layout.
Output: Animated hero, shimmer CSS, SmoothScroll in layout.
</objective>

<execution_context>
@C:\Users\Lucas Benutzer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Lucas Benutzer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-media/04-RESEARCH.md
@components/shared/fade-in-section.tsx
@components/smooth-scroll.tsx
@app/[locale]/page.tsx
@app/[locale]/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroAnimated client component and add shimmer CSS</name>
  <files>components/shared/hero-animated.tsx, app/globals.css, app/[locale]/page.tsx</files>
  <action>
1. Create `components/shared/hero-animated.tsx` as a "use client" component:
   - Accept props: `headline: string`, `subheadline: string`, `cta: ReactNode`
   - Use Framer Motion `motion.div` with `initial="hidden" animate="visible"`
   - Define `fadeUp` variant: hidden `{ opacity: 0, y: 30 }`, visible with custom delay `(i) => ({ opacity: 1, y: 0, transition: { duration: 0.6, delay: i * 0.15, ease: [0.22, 1, 0.36, 1] } })`
   - Wrap headline in `motion.h1` with `custom={0} variants={fadeUp}`, keep existing classes `text-4xl font-bold tracking-tight md:text-6xl`
   - Wrap subheadline in `motion.p` with `custom={1} variants={fadeUp}`, keep existing classes
   - Wrap cta in `motion.div` with `custom={2} variants={fadeUp}`

2. Add shimmer CSS to `app/globals.css`:
   - Add `@keyframes shimmer` that moves `background-position` from -200% to 200%
   - Add `.shimmer-btn` class: `background: linear-gradient(90deg, transparent 40%, oklch(0.7 0.15 260 / 0.3) 50%, transparent 60%)`, `background-size: 200% 100%`, `animation: shimmer 3s ease-in-out infinite`
   - This is a subtle overlay shimmer for buttons, NOT text-fill shimmer

3. Update `app/[locale]/page.tsx`:
   - Import `HeroAnimated` from `@/components/shared/hero-animated`
   - Replace the static h1/p/div with `<HeroAnimated headline={t("headline")} subheadline={t("subheadline")} cta={<Button asChild size="lg"><Link href="/technology">{t("cta")}</Link></Button>} />`
   - Keep the page as a server component (no "use client") — HeroAnimated is the client boundary
   - Keep the outer section and radial glow div unchanged
   - Add `shimmer-btn` class to the Button or its wrapper for the shimmer effect
  </action>
  <verify>Run `npx next build` — no errors. Open home page in browser, hero text should animate in with staggered fade-up.</verify>
  <done>Home page hero animates with staggered fade-up on load and CTA has shimmer effect.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SmoothScroll into locale layout</name>
  <files>app/[locale]/layout.tsx, components/smooth-scroll.tsx</files>
  <action>
1. Open `app/[locale]/layout.tsx`
2. Import `SmoothScroll` from `@/components/smooth-scroll`
3. Wrap the content inside `<body>` with `<SmoothScroll>`:
   ```
   <body className={...}>
     <NextIntlClientProvider messages={messages}>
       <SmoothScroll>
         <div className="noise-overlay" aria-hidden="true" />
         <Navbar />
         <main>{children}</main>
         <Footer />
       </SmoothScroll>
     </NextIntlClientProvider>
   </body>
   ```
4. Check `components/smooth-scroll.tsx` — if it doesn't accept children or has issues, fix it. It should be a "use client" component that initializes Lenis and renders children.
5. Do NOT change the existing static `metadata` export in this task — that will be handled in plan 04-02.
  </action>
  <verify>Run `npx next build` — no errors. Open any page, scroll should feel smooth (not default jerky). Verify FadeInSection animations on content pages still work.</verify>
  <done>Lenis smooth scroll is active on all pages. Existing animations unaffected.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Home page: hero text animates in with visible stagger delay between headline, subheadline, and CTA
3. Home page: CTA button has subtle shimmer animation
4. Any content page (e.g. /technology): FadeInSection scroll-triggered animations still work
5. Scrolling on any page feels smooth (Lenis active)
</verification>

<success_criteria>
- HeroAnimated component renders with Framer Motion fade-up animation
- Shimmer CSS keyframes exist in globals.css and are applied to CTA
- SmoothScroll wraps layout content and Lenis is active
- Build passes, no regressions on existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-media/04-01-SUMMARY.md`
</output>
